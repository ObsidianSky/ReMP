import React from 'react';
import './TopBar.scss'
import Logo from '../../common/Logo/Logo';

const TopBar = () => (
    <div className="top-bar">
        <Logo/>
        {/*link will be here*/}
    </div>
);

export default TopBar;